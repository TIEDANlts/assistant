# 验证 5：推送通道

状态：待验证。结论同步到 ADR 0005。

运行 `uv run python spikes/push_probe.py --channel all`（至少配置两个通道），在一台 iOS 和一台安卓设备上各确认一次；想看稳定性可以加 `--repeat 3`。

| 通道 | 接口 | iOS：收到？延迟 | 安卓：收到？延迟 | 锁屏显示 | 点开链接 |
| --- | --- | --- | --- | --- | --- |
| 待填写 | | | | | |

注意：

- 锁屏上显示的就是推送里的全部文字，所以将来的推送只放标题和不含个人信息的摘要。
- 链接能否打开取决于 PWA 的地址（Tailscale 或 HTTPS 域名），Phase 2 部署后再验证一次。

## 默认通道

- 选定：待定
- 理由：待定
